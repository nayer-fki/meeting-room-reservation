apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "meeting-room-reservation.fullname" . }}-test-connection"
  labels:
    {{- include "meeting-room-reservation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "meeting-room-reservation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
